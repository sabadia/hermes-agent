name: hindsight
version: 1.1.0
description: "Hindsight — long-term memory with knowledge graph, entity resolution, and multi-strategy retrieval. Now with sync_retain support and ollama-cloud provider."
pip_dependencies:
  - "hindsight-client>=0.4.22"
requires_env: []
hooks:
  - on_session_end
